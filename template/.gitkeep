{% extends "base.html" %}

{% block content %}
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">📊 Student Overview</h5>
                <p class="card-text">
                    Total students: <strong>{{ system.students|length }}</strong>
                </p>
                <a href="{{ url_for('download_all') }}" class="btn btn-success mb-3">
                    ⬇️ Download All Results
                </a>
                {% if system.students %}
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Reg No</th>
                                <th>Name</th>
                                <th>Course</th>
                                <th>GPA</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for reg_no, data in system.students.items() %}
                            <tr>
                                <td>{{ reg_no }}</td>
                                <td>{{ data.name }}</td>
                                <td>{{ data.course }}</td>
                                <td>
                                    {# Calculate GPA using the system's method #}
                                    {{ system.calculate_gpa(reg_no) }}
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                {% else %}
                <div class="alert alert-info mt-3">
                    No students registered yet. Use the <strong>Add Student</strong> menu to get started.
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
